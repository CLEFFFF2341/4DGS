# P08/P09 依赖阻塞记录

P08 与 P09 当前均为 `BLOCKED_CACHE`，不是负结果。

- P08 强制依赖 K3 的逐 ray、按 patch、完整有序 alpha 列表以及可重新计算透射率的颜色重放；现有 K2 只在 CUDA 内部汇总单删平方误差，未导出这些列表。用 K1/K2 聚合量或冻结原始透射率替代会改变预注册方法定义。
- P09 强制依赖 K3 中有符号的逐 ray RGB 删除向量来拟合 DC Jacobian 并严格划分 fit/check；现有 K2 仅有平方后的 `e_it`，无法恢复误差符号。用平方分数拟合被 prompt 明确禁止。
- K3 在 P00 状态中尚未实现，P04 也已因同一缺口标记 `BLOCKED_CACHE`。本批保留该共同缺口并转向不依赖 K3 的 P10；没有用低保真替代物生成伪结果。

所需解锁工作是新增并验证 K3 CSR/ray-list CUDA 导出、16-ray patch 采样、真实重放与 S3 误差门槛；完成后 P04/P08/P09/P20 可共享同一缓存。

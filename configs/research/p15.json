{
  "schema": "p15-config-v1",
  "scene": "cut_roasted_beef",
  "target_variable_fraction": 0.5,
  "seed_policy": "deterministic; verify outputs for seeds 0,1,2 and evaluate once",
  "rules": ["R0", "R1"],
  "finetune_steps": 0,
  "index_dtype": "uint16"
}
